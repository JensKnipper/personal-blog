---
draft: true
title: 
date: 2026-01-19T01:00:00+00:00
description: 
categories:
---
java sane null handling
layers of multitenancy



sqlite multiple connections in memory
hibernate:
  dialect: "org.hibernate.community.dialect.SQLiteDialect"
  current_session_context_class: "thread"
  show_sql: true
  format_sql: true
  hbm2ddl:
    auto: "create-drop"
  connection:
    driver_class: "org.sqlite.JDBC"
    url: "jdbc:sqlite:file::memory:?cache=shared"
    username: "sa"
    password: "sa"
    pool_size: 3
        <dependency>
            <groupId>org.hibernate.orm</groupId>
            <artifactId>hibernate-core</artifactId>
            <version>${hibernate.version}</version>
        </dependency>
        <dependency>
            <groupId>org.hibernate.orm</groupId>
            <artifactId>hibernate-community-dialects</artifactId>
            <version>${hibernate.version}</version>
        </dependency>
        <dependency>
            <groupId>org.xerial</groupId>
            <artifactId>sqlite-jdbc</artifactId>
            <version>3.49.1.0</version>
        </dependency>
        try (Connection connection = DriverManager.getConnection(databaseConfiguration.url(), databaseConfiguration.user(), databaseConfiguration.password())) {
                Statement statement = connection.createStatement();
                ResultSet resultSet = statement.executeQuery(sql);
        }
